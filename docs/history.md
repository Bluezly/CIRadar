# Development history

CI Radar was developed before its first public GitHub push.

The pre-public history in this repository was reconstructed from dated source snapshots and archive file timestamps preserved during development. Commit boundaries from that period are approximate; the file contents and milestone states come from the archived snapshots. Commits made after the public import are ordinary Git history.
